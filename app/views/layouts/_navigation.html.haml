-# navigation styled for Zurb Foundation 5
-# the syntax for data-topbar may need to change for Zurb Foundation 5
.contain-to-grid
  %nav.top-bar{"data-topbar" => ""}
    %ul.title-area
      %li.toggle-topbar.menu-icon
        %a{:href => "#"}
          %span Menu
    %section.top-bar-section
      / Right Nav Section
      %ul.right
        - ParentPage.all.each do |ppage|
          %li.has-dropdown
            %a{href: "#"}= ppage.title
            %ul.dropdown
              - ppage.child_pages.each do |cpage|
                %li
                  = link_to cpage.title, cpage

        /%li.has-dropdown
        /  %a{href: "#"} Településünk
        /  %ul.dropdown
        /    %li
        /      %a{href: "#"} Proba
        %li{class: "#{"active" if controller.controller_name == "archives"}"}
          = link_to "Archívum", archives_path
        %li
          %a{href: "#"} Vendégváró
      / Left Nav Section
      %ul.left
        %li{class: "#{"active" if controller.controller_name == "posts"}"}
          = link_to "Aktuális", root_path